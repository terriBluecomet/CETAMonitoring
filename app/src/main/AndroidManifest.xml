<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.practice.cetamonitoring">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".AddReport" />
        <activity android:name=".ProjectInfo" />
        <activity android:name=".ProjectsPerProvince" />
        <activity android:name=".SelectProvince" />
        <!--<activity android:name=".SelectEntity" />-->
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainMenu" />

        <activity android:name=".SelectEntity"
            android:screenOrientation="landscape"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>